import { createTheme } from "@mui/material";

export const pokemonTheme = createTheme({
    palette: {
        primary: {
            main: "#f00000", 
            
        },
        secondary: {
            main: "#ffffff"

        }
    }
}) 